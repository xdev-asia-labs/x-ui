#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Running tests before push..."

# Run tests only (build is handled by CI/CD)
pnpm test || {
  echo "❌ Tests failed! Push aborted."
  exit 1
}

echo "✅ Tests passed! Proceeding with push..."
echo "📦 Build will be verified by CI/CD."
